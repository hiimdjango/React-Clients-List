import React from 'react';
import classes from './Header.module.css';

const header = () => (
    <header id="topPage" className={classes.Header}>
        <h1>Our Clients</h1>
    </header>
);

export default header;